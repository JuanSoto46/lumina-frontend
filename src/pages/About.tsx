import React from "react";
export default function About() {
  return (
    <section>
      <h2>About us</h2>
      <p>Student project built with React + Vite + TypeScript + SASS. WCAG: perceivable text and keyboard operability.</p>
    </section>
  );
}
